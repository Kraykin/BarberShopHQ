<h2>Контакты</h2>

<p>Если у вас есть вопрос к нам, вы можете его задать с помощью данной формы.</p>

<form action="/contacts" method="POST">
  
  <div class="form-group">
    <label>E-Mail</label>
    <input name="client[email]" type="email" class="form-control" placeholder="Напишите Ваш e-mail адрес" value="<%= @contact[:email] %>">
  </div>
  
  <div class="form-group">
  	<label>Сообщение</label>
  	<textarea name="client[contact]" class="form-control" rows="3" placeholder="Напишите Ваше сообщение" value="<%= @contact[:contact] %>"></textarea>
  </div>
  
  <button type="submit" class="btn btn-primary">Отправить</button>

</form>