<h2>Записаться</h2>
<p>На этой странице вы сможете записаться к нам в Barber Shop</p>


<script type="text/javascript">

$(function() {

  $('#color').simplecolorpicker();
  $('#datetimepicker').datetimepicker();

});

</script>


<form action="/visit" method="POST" role="form">

  <div class="form-group">
    <label>Ваше имя</label>
    <input name="client[name]" type="text" class="form-control" placeholder="Напишите Ваше имя" value="<%= @appointment[:name] %>">
  </div>

  <div class="form-group">
    <label>Ваш телефон</label>
    <input name="client[phone]" type="text" class="form-control" placeholder="Напишите номер Вашего телефона" value="<%= @appointment[:phone] %>">
  </div>

  <div class="form-group">
    <label>Когда Вас ждать?</label>
    <input id="datetimepicker" name="client[datestamp]" type="text" class="form-control" placeholder="Выберите дату и время Вашего визита" value="<%= @appointment[:timestamp] %>">
  </div>


  <div class="form-group">
    <label>Выберите парикмахера</label>
    <select name="client[barber]" class="form-control">
    
      <% @barbers.each do |item| %>
        <option <%= @appointment[:barber] == item.name ? 'selected' : '' %>><%= item.name %></option>
      <% end %>

    </select>
  </div>

<div class="form-group">
    <label>Цвет краски: </label>
    <select id="color" name="client[color]">
      <option <%= @appointment[:color] == '#7bd148' ? 'selected' : '' %> value="#7bd148">Green</option>
      <option <%= @appointment[:color] == '#5484ed' ? 'selected' : '' %> value="#5484ed">Bold blue</option>
      <option <%= @appointment[:color] == '#a4bdfc' ? 'selected' : '' %> value="#a4bdfc">Blue</option>
      <option <%= @appointment[:color] == '#46d6db' ? 'selected' : '' %> value="#46d6db">Turquoise</option>
      <option <%= @appointment[:color] == '#7ae7bf' ? 'selected' : '' %> value="#7ae7bf">Light green</option>
      <option <%= @appointment[:color] == '#51b749' ? 'selected' : '' %> value="#51b749">Bold green</option>
      <option <%= @appointment[:color] == '#fbd75b' ? 'selected' : '' %> value="#fbd75b">Yellow</option>
      <option <%= @appointment[:color] == '#ffb878' ? 'selected' : '' %> value="#ffb878">Orange</option>
      <option <%= @appointment[:color] == '#ff887c' ? 'selected' : '' %> value="#ff887c">Red</option>
      <option <%= @appointment[:color] == '#dc2127' ? 'selected' : '' %> value="#dc2127">Bold red</option>
      <option <%= @appointment[:color] == '#dbadff' ? 'selected' : '' %> value="#dbadff">Purple</option>
      <option <%= @appointment[:color] == '#e1e1e1' ? 'selected' : '' %> value="#e1e1e1">Gray</option>
    </select>
  </div>

  <button type="submit" class="btn btn-primary">Записаться</button>

</form>
